const classrooms = require('./classrooms')
const users = require('./users')
const sessions = require('./sessions')
const classroomPlayers = require('./classrooms/players')

module.exports = {
  classrooms,
  users,
  sessions,
  classroomPlayers,
}
